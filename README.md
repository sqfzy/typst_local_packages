Personal typst packages. To use it:
```
mkdir -p ~/.local/share/typst/packages 
cd ~/.local/share/typst/packages 
git clone https://github.com/sqfzy/typst_local_packages.git 
mv typst_local_packages local
```
